import 'package:flutter/material.dart';

class ValidasiData extends StatelessWidget {
  const ValidasiData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  MaterialApp(
      title:'halaman login',
      theme: ThemeData(
        primarySwatch: Colors.lightBlue,
      ),
      // home: MyHope(),
    );
    
  }
}